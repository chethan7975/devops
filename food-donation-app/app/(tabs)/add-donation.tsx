import { View, Text } from 'react-native';

export default function AddDonationScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Add a Food Donation (form coming soon)</Text>
    </View>
  );
}